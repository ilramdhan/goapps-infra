apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: iam-service
  labels:
    app: iam-service
spec:
  selector:
    matchLabels:
      app: iam-service
  namespaceSelector:
    any: true
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
