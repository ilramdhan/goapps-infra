apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: iam-service
spec:
  tls:
    - hosts:
        - goapps.mutugading.com
      secretName: goapps-tls
  rules:
    - host: goapps.mutugading.com
