# Staging: Lower resource limits
apiVersion: apps/v1
kind: Deployment
metadata:
  name: finance-service
spec:
  template:
    spec:
      containers:
        - name: finance-service
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "1.5Gi"
              cpu: "500m"
