apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: finance-cost-worker
  labels:
    app: finance-cost-worker
    release: prometheus
spec:
  selector:
    matchLabels:
      app: finance-cost-worker
  namespaceSelector:
    any: true
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
