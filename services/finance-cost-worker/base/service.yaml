apiVersion: v1
kind: Service
metadata:
  name: finance-cost-worker
  labels:
    app: finance-cost-worker
spec:
  type: ClusterIP
  ports:
    - name: health
      port: 8083
      targetPort: 8083
    - name: metrics
      port: 8093
      targetPort: 8093
  selector:
    app: finance-cost-worker
